import ReorderIdeaPageClient from '@/app/[userCode]/list/[id]/reorder/client';

export const dynamic = 'force-static';

export default function ReorderIdeaPage() {
  return <ReorderIdeaPageClient />;
}
