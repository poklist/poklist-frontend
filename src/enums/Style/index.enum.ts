export enum RadioType {
  DEFAULT = 'default',
  BUTTON = 'button',
}

export enum MessageType {
  SUCCESS = 'success',
  ERROR = 'error',
}

export enum DropdownItemType {
  ITEM = 'item',
  SEPARATOR = 'separator',
}
