export enum EditFieldVariant {
  TEXT = 'text',
  IMAGE = 'image',
}

export enum FieldType {
  DISPLAY_NAME = 'displayName',
  USER_CODE = 'userCode',
  BIO = 'bio',
  PROFILE_IMAGE = 'profileImage',
}
